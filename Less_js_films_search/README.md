1. site for searching movies by title.
2. instead of the standard css, I used the less.js library with its features. 
   But less only works on localhost! (or change the less extension to standard css ).
